<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="loomo.com.awesome">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <service android:name=".service.TriggerService" />

        <activity
            android:name=".WorkActivity"
            android:label="@string/title_activity_work"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OperatorActivity"
            android:label="@string/title_activity_operator"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <meta-data android:name="packageTo2" android:value="com.example.a8051.remotecontrol"></meta-data>

    </application>

</manifest>